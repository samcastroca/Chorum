# ---- Secrets / local config ----
.env
.env.*
!.env.example

# ---- Python / backend ----
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
# uv creates the venv inside the project; the lockfile IS committed.
# (uv.lock is intentionally tracked — do not ignore it.)

# Local SQLite databases (dev)
*.db
*.sqlite
*.sqlite3

# ---- Node / frontend ----
node_modules/
frontend/dist/
frontend/dist-ssr/
*.local
.vite/
vitest-report/
coverage/

# ---- Docker ----
# (volumes are named and live outside the repo)

# ---- Editors / OS ----
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
desktop.ini
