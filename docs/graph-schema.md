# Esquema del grafo (`GraphDefinition`)

> **Stub (Fase 0).** Se completa en la **Fase 1**, cuando se diseñe el esquema Pydantic del
> grafo de agentes (nodos `agent`/`tool`/`condition`/`code`/`human_in_loop`/`start`/`end`,
> edges con condición opcional, y esquema del estado compartido).

El `GraphDefinition` es la única fuente de verdad sobre qué puede ejecutar el sistema
(invariante 1 de [CLAUDE.md](../CLAUDE.md)).
